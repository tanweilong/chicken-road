# M7.1b — Option B Re-baseline (FINAL), uiux report

Status: DONE. This is the durable record for the chat report; see the
🟢/🔵/🟠/✅ sections in the orchestrator's relay for the full checklist.

Scope: re-baseline `docs/ASSET_MANIFEST.md` and `docs/DESIGN.md` (§0/§1/§10)
to Option B (fully smooth/illustrated world art) as FINAL, per
`docs/PLAN.md` §7.12 and `docs/reports/ISSUES.md` SCOPE-03 (RESOLVED)/
SCOPE-04. M7.7 (chrome/HUD/particle restyle) explicitly NOT touched.
index.html / frontend code NOT touched.

## Files changed
- `docs/ASSET_MANIFEST.md` — Global Rules 1/2/4/5 revised in place; new
  "Option-B Payload-Ceiling & Hue-Family-Guide Framework" section added;
  per-asset native-size cells updated to payload ceilings across Sections
  1-8; Section 1 (`chick-idle`) negative prompt strengthened; Section 8
  (parallax) subject-rendering language de-contradicted (hard-edge language
  removed from the 6 per-asset prompts, backdrop instructions kept
  unchanged); Section 7 intro + A3 check #4 carry an explicit Option-B
  heightened tiling-risk flag; A3 checks #1/#2/#3/#7 rewritten, #5 lightly
  adapted, #4/#6/#8 otherwise unchanged; A4 validation batch + GO/NO-GO
  paragraph updated to match; Summary counts section marks Option B FINAL.
- `docs/DESIGN.md` — §0 rewritten to record the FINAL Option B decision as a
  deliberately sequenced two-track identity (illustrated world / still-hard
  chrome pending M7.7), explicitly cross-referencing SCOPE-04/M7.7 so it
  does not read as an internal contradiction; §1.1 gets one added note
  flagging that the "16×16 × 3× = 48px tile" derivation is now historical
  (runtime grid constants themselves untouched, that's M7.7/frontend
  territory); new §10.5 appended after §10.4 (history preserved) logging
  the SCOPE-03→decision→re-baseline sequence and the `chick-idle`
  adopt-vs-regenerate verdict.

## `chick-idle` verdict (task item #3)
`assets/gen/m7.1b-styleB/final/chick-idle-styleB.png` (128×128 RGBA,
chroma-keyed, no downscale/quantize) — checked pixel-by-pixel against the
finalized manifest. Size/no-quantize conform. **NOT adoptable as-is** — 2
defects:
1. Baked-in grass/ground ellipse under the chicken's feet (genuine
   model-drawn content, not a keying artifact) — violates "no scenery, no
   shadow." Needs a fresh generation call with a strengthened negative
   prompt (already added to the manifest this task).
2. A fully opaque, un-keyed trapped patch of background-key color between
   the legs — a border-connected-flood-fill blind spot for concave
   silhouettes. Fixable by reprocessing the existing raw source with a
   corrected global color-distance keying pass (documented as a Global Rule
   2a mechanism fix this task) — no new model call needed for this one
   alone.
Net: regenerate once with both fixes together (same seed `1101`), rather
than reprocess-then-regenerate twice. Flagged to the orchestrator per task
instructions — uiux has no working HF MCP tools (ENV-01 still open).

## Issues / flags for the orchestrator to log
- Heightened tiling risk under Option B (A3 check #4) — documented in both
  files as an accepted, budgeted risk, not a blocker; expect a higher
  tile-fallback rate than the M7.1 hard-pixel batch.
- The two `chick-idle-styleB` defects above (grass ellipse, trapped keying
  pocket) — both written into ASSET_MANIFEST.md (negative prompt + Rule 2a)
  and DESIGN.md §10.5 as fixes for the *next* generation pass, not
  self-resolving.
- `DESIGN.md` §4 (Sprite Specs, e.g. §4.1 "Chicken (16×16...)") still
  describes the old pixel-grid sprite specs verbatim — out of this task's
  explicit scope (only §0/§1/§10 were authorized), but it will read stale
  once M7.2 generates Option-B assets. Recommend a follow-up note or fold
  into a future curation pass (the manifest's original plan for "uiux
  curates the results afterward in a new DESIGN.md §10.5 (M7.3)" now needs
  a different section number since §10.5 is used by this task's decision
  log — suggest §10.6 for that future full-batch curation).
