# M7.2 Curation (A5) — uiux report

Task: formal A3 curation pass over all 34 M7.2-generated image assets, per
`docs/PLAN.md` §7.13 and `docs/ASSET_MANIFEST.md` A3. Full write-up lives in
`docs/DESIGN.md` §10.6 (this file is the durable status record).

## 🟢 START — TODO checklist

- [x] Read `docs/reports/ISSUES.md`, `docs/PLAN.md` §7.13, `docs/ASSET_MANIFEST.md`
      (A3 checklist, Option-B revised), `docs/DESIGN.md` §10 history.
- [x] Run all 8 A3 checks against every one of the 34 `assets/gen/m7.2/final/*.png`
      files (pixel-level alpha/residue/component analysis + zoomed visual review).
- [x] Mandatory full 3×3 tiling test on all 11 ground tiles (not a sample) —
      confirm/overturn the 2 orchestrator-flagged tiles + evaluate the other 9.
- [x] Judge the other 23 non-tile assets per the A3 checklist; default-accept
      cosmetic misses, flag only real defects.
- [x] Produce the curation verdict table as `docs/DESIGN.md` §10.6.
- [x] Produce the accepted-asset registry manifest for frontend integration.
- [x] Note SFX is out of scope (ENV-03, deferred) — not touched.

## 🔵 PROGRESS

- Built a Python/Pillow analysis pass (alpha completeness, key-color-residue
  distance metric, connected-component scan) across all 34 finals, cross-checked
  every automated flag with a zoomed, checkerboard-composited visual read (the
  automated metric produced known false positives on near-black/near-white
  subjects and green-subject/magenta-key pairs — resolved by trusting the visual
  check in every such case, documented per-asset in §10.6).
- Rendered an actual 3×3 tile composite (both native and zoomed) for all 11
  ground tiles, plus targeted 1×2 vertical/horizontal seam crops for every tile
  with a non-trivial edge-continuity signal.
- Rendered a readability grid (chicken/eagle/vehicle set at true in-game size,
  light bg + dark bg) to check A3 #5.

## 🟠 ISSUES (new/confirmed findings from this task — logged to ISSUES.md as ISS-05)

1. **`tile-s2-road` — NEW finding, not in the orchestrator's original 2-item
   flag list.** Visible vertical-tiling FAIL: an internal top-to-bottom gradient
   baked into the tile resets abruptly at every tile boundary, producing a
   repeating horizontal "banding" pattern. Confirmed both visually (1×2 zoomed
   stack) and numerically (top/bottom edge diff ≈28 vs. tile's own std ≈30).
   **Severity: Minor** (fallback-first absorbs it at zero cost — existing
   programmatic S2-road fill stays wired, no code change needed). Owner:
   none (no fix required to ship; a future regen could target this if desired).
2. **`tile-s3-road` / `tile-s3-rail` — CONFIRMED** the orchestrator's original
   spot-check flags on formal re-check (uneven/blurry diagonal hazard stripes;
   asymmetric top-heavy rail structure, respectively). Same zero-cost fallback
   applies.
3. **`veh-s2-truck`, `veh-s3-truck`, `eagle-grabbed` — baked-in ground/shadow
   element** (green grass patch under the van; tan ellipse under the truck
   wheels; tan ellipse under the eagle's talons) — the same recurring
   Option-B "grounded subject gets an implied ground plane" failure mode
   DESIGN §10.5 already diagnosed and fixed for `chick-idle`, recurring here
   because Sections 2/6's per-asset negative prompts weren't given the same
   strengthened ground/shadow exclusion terms Section 1 got. **Severity:
   Major-ish but zero product risk** — fallback-first means the existing
   programmatic `Vehicle`/`Eagle` draw stays live until regenerated; not
   blocking ship. Owner: orchestrator (regen, when scheduled) — needs the
   strengthened negative terms (already documented in Global Rule 4 / §10.5)
   copied into these 3 rows' own per-asset negative-prompt text.
4. **`bg-s2-skyline` — chroma-key never applied.** The final PNG's alpha
   channel is 0% transparent; the entire `#00FF00` backdrop is still fully
   opaque. Confirmed this is a pipeline-execution miss (the raw `.webp` source
   is clean and trivially keyable), not a style/model failure. **Severity:
   Major (unshippable as-is) but cheap to fix** — a reprocess of the existing
   raw file through the standard Rule 2a keying step, no new generation call
   needed. Owner: orchestrator (reprocess).

All 4 "reject-regenerate-needed" items are fallback-safe: the existing
programmatic draw call for each stays wired and renders correctly until a
fix lands, per PLAN §7.2 Constraint 5 / Global Rule 8. None block ship of the
other 27 assets.

## ✅ DONE

- [x] All 8 A3 checks run against all 34 assets.
- [x] Full 3×3 tiling test on all 11 tiles — both orchestrator flags CONFIRMED,
      1 additional fail found (`tile-s2-road`), 8 tiles ACCEPT (1 with note).
- [x] All 23 non-tile assets judged; cosmetic misses defaulted to accept per
      pm's instruction; 3 real defects flagged (not mass-regenerated).
- [x] Curation verdict table written to `docs/DESIGN.md` §10.6 (§10.6.1-10.6.9).
- [x] Asset registry manifest written to `docs/DESIGN.md` §10.6.10 (JSON block,
      27 accepted assets, id → path → replaces → mode).
- [x] SFX untouched, noted as deferred (ENV-03) where relevant context appears.

**Result: 25 ACCEPT / 2 ACCEPT-WITH-NOTE / 3 REJECT-FALLBACK-TO-PROGRAMMATIC
(tiles) / 4 REJECT-REGENERATE-NEEDED.**

**Deliverables:**
- `docs/DESIGN.md` §10.6 (full curation table + registry) —
  `/Users/wl/wl/ai-workspace/projects/chicken-road/docs/DESIGN.md`
- This report —
  `/Users/wl/wl/ai-workspace/projects/chicken-road/docs/reports/m7.2-uiux-curation-a5.md`

**Handoff notes for frontend (M7.4):**
- Wire the 27-asset JSON registry in DESIGN §10.6.10 directly — id → base64
  the listed `final/*.png` → draw in place of the cited programmatic call,
  fallback-first (registry absent → keep existing draw, no deletion).
- Do NOT wire `tile-s2-road`, `tile-s3-road`, `tile-s3-rail`,
  `veh-s2-truck`, `veh-s3-truck`, `eagle-grabbed`, `bg-s2-skyline` — leave
  their existing programmatic draws untouched; they'll slot into the registry
  automatically in a follow-up increment once fixed, no code change required
  on frontend's side when that happens.
- Aggregate accepted payload well inside budget (~512KB raw across all 34,
  even less once the 7 non-accepted are excluded) — no R11 payload concern.
