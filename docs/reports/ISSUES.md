# Issue Ledger — chicken-road

Durable record of every issue found. Statuses: OPEN → IN-PROGRESS → FIXED (or WON'T-FIX + reason).

| ID | Description | Severity | Owner | Status | Found | Resolved |
|----|-------------|----------|-------|--------|-------|----------|
| BUG-1 | `score` not reset to 0 on restart — stale score persists (AC-10) | Major | frontend | FIXED (`322b8cf`, verified Cycle 2) | 2026-07-23 | 2026-07-23 |
| BUG-2 | Train warning bell fires ~87× instead of ~5× per warning | Major | frontend | FIXED (`120932e`, verified Cycle 2) | 2026-07-23 | 2026-07-23 |
| BUG-3 | QA hook crashes on non-numeric `?stage=abc` | Minor | frontend | FIXED (`ce5553a`, verified Cycle 2) | 2026-07-23 | 2026-07-23 |
| BUG-4 | Buses spawn in Stage 3 (spec: Stage 2 only) | Minor | frontend | FIXED (`8ca090f`, verified Cycle 2) | 2026-07-23 | 2026-07-23 |
| BUG-5 | `hops=N` QA hook teleports onto occupied hazard tiles | Minor | frontend | FIXED (`ce5553a`, verified Cycle 2) | 2026-07-23 | 2026-07-23 |
| BUG-6 | Test harness has no `package-lock.json` / declared devDependencies — clean-room `npm ci` for the QA scripts would fail | Cosmetic | frontend | WON'T-FIX (dev-tooling only; shipped `index.html` is zero-dependency/zero-network — does not affect product) | 2026-07-23 | 2026-07-23 |
| ISS-01 | Game feel (hop crispness, camera tension, death punch) — #1 brief priority, needs human playtest | Major | frontend | OPEN (human-playtest only) | 2026-07-23 | — |
| ISS-02 | Audio audibility + mute across desktop/mobile (AC-11) needs real-device verify | Minor | frontend | OPEN (human-playtest only) | 2026-07-23 | — |
| ISS-03 | Touch/swipe on a real device (AC-13); 24px swipe threshold may need tuning | Minor | frontend | OPEN (human-playtest only) | 2026-07-23 | — |
| ISS-04 | Canvas centering/crispness on real screens | Minor | tester | FIXED | 2026-07-23 | 2026-07-23 |
| SCOPE-01 | Client-directed scope change (NOT a bug): product owner authorized an exception allowing Hugging Face MCP-generated raster assets for chicken-road, superseding PRD §5's "all sprites drawn programmatically / no external images." Reconciled with the still-binding no-runtime-network-calls rule by requiring any generated images be inlined as base64 `data:` URIs in the shipped single HTML file (asset generation at authoring time only; zero runtime fetch). PRD §5 amended 2026-07-24. DESIGN.md §10 to be updated by uiux after the asset-generation test. | n/a (scope change) | user | FIXED (decision made 2026-07-24; PRD §5 amended) | 2026-07-24 | 2026-07-24 |
| SCOPE-02 | Client-directed scope change (NOT a bug): product owner approved a Hugging Face MCP-generated SFX sample (`assets/test-sfx-hop.flac`, `hkchengrex/MMAudio`) and directed that generated audio assets be used and WIRED INTO the shipped game, superseding PRD §5's "All SFX synthesized via Web Audio API." Reconciled with the still-binding no-runtime-network-calls rule the same way as SCOPE-01: generated audio inlined as base64 `data:` URIs, decoded to an in-memory buffer via Web Audio (zero runtime fetch). Web Audio remains the playback/mute engine; synthesis stays as a per-SFX fallback. PRD §5 + AC-11 amended 2026-07-24 (AMENDMENT #2). | n/a (scope change) | user | FIXED (client-CONFIRMED 2026-07-24 — "replace with the new assets"; PRD §5/AC-11 amended) | 2026-07-24 | 2026-07-24 |
| ENV-01 | Subagent Hugging Face MCP tool-access gap: uiux (and any) subagent spawns find no `mcp__hugging-face__*` tools bound to their runtime schema, though the `hugging-face` MCP server is connected/authenticated at the session level. Only the orchestrator's main session can call HF generation tools. Environment/config limitation, not fixable from within a subagent turn. IMPACT ON PLAN: all M7 asset-generation steps route through the orchestrator (uiux specs prompts + curates results; orchestrator executes generation). | Major (blocks self-serve generation) | orchestrator | OPEN (worked-around by routing generation through orchestrator; env fix tracked, unresolved) | 2026-07-24 | — |
| ENV-02 | Mandated image-gen space `lm555/game-asset-generator` is not usable via either access path: (1) via the authenticated `dynamic_space` MCP tool the /generate call SUCCEEDS but returns a raw in-memory `PIL.Image` object with no file path/URL — no way to extract image bytes (unlike FLUX.1-Schnell/MMAudio which return downloadable file refs); (2) via anonymous `gradio_client` every call fails with a generic AppError (anon-tier quota/session issue), and attaching the account token to that client was correctly AVOIDED (no raw-credential handling, per Security Rules). Not a style/prompt problem. RESOLUTION: pivot the image pipeline to `evalstate/flux1_schnell` (proven, authenticated, returns retrievable files) as the SOURCE generator + the manifest's already-mandatory Global Rule 2 post-process (chroma-key → downscale → palette-quantize to exact DESIGN §3 hex → nearest-neighbor native-grid PNG), which imposes the hard-edged pixel grid deterministically in our own code. This is exactly DESIGN §10.2's sanctioned remediation path (b). lm555 kept as "revisit only if access is fixed." | Major (blocked M7.1 validation batch) | orchestrator (exec) + uiux (manifest Global Rule 1 revision) | IN-PROGRESS (pivot validated 2026-07-24: batch 3/4 PASS — chick-idle/veh-s1-car/tile-s1-grass; bg-s1-hill-far FAILED on a 2nd sub-bug: green target palette collides with the #00FF00 chroma-key so the hill keyed out. FIX: uiux sets a per-asset key color ABSENT from + maximally distant from that asset's own palette (magenta #FF00FF for green-dominant assets — grass/river/lily/corn-husk; note lily also has a pink flower, so pick per-asset, not a blanket magenta). Re-run bg-s1-hill-far ONLY; other 3 stand. R15 sprite-quality: chick-idle borderline-muddy but usable — no client escalation needed) | 2026-07-24 | — |
| ENV-03 | M7.2 SFX generation blocked on `hkchengrex/MMAudio` (0/10): (1) space hard-min clip duration 1.0s vs manifest's 0.15-0.9s specs; (2) after 1 call, ANONYMOUS ZeroGPU quota wall ("120s requested vs 0s left; authenticate with a token") — same anon-tier limit as ENV-02. PM RESOLUTION: (1) accept 1.0s clips, frontend trims/fast-fades at playback (decode buffer, play first N ms) — no new space needed; (2) FIRST retry MMAudio via the ACCOUNT-AUTHENTICATED MCP path (the same mechanism FLUX used, where the MCP server holds the credential) rather than anonymous gradio_client — this uses account quota with NO raw-credential handling. DECLINED option (c) raw HF token in gradio_client — Security Rules (never read/print/log a secret); not PM-authorizable. If authenticated-MCP unavailable/exhausted → DEFER generated SFX to a follow-up pass (option d) and ship images-first; the game keeps its existing Web-Audio-SYNTHESIZED SFX as the live fallback (SCOPE-02 preserved synthesis as fallback), so deferral has ZERO product-break risk. | Major (blocks generated SFX only) | orchestrator (exec) | DEFERRED to fast-follow (2026-07-24). Authenticated-MCP retry RULED OUT structurally: MMAudio lacks the Hub `mcp-server` tag, so `dynamic_space` 404s regardless of credentials (not a quota/auth issue). Raw-token gradio_client stays DECLINED (Security). So this pass ships IMAGES-ONLY; game keeps existing Web-Audio-SYNTHESIZED SFX as the live fallback (zero product break). REVISIT paths for the follow-up increment: (i) an MCP-tagged sub-second-capable audio Space, (ii) accept 1.0s MMAudio clips IF the anon quota resets and frontend trims/fades, or (iii) user enables HF token auth in the env (user/env action, not agent secret-handling). Client to be informed generated SFX = fast-follow, not dropped | 2026-07-24 | — |
| BUG-7 | Client-reported (mid-task, verbatim): "when the chicken cross river, it's hard to jump on the wood or the leaves." Landing on a moving Log / lily-pad in the river lane is too hard — suspected too-tight landing hitbox/tolerance, or a log-carry snap / hop-resolve-timing mismatch vs. the moving platform's position. Core river-crossing mechanic (AC-5) + game-feel (#1 brief priority). SHIPPED-game bug, INDEPENDENT of M7. | Major | frontend | FIXED, tester-verified, MERGED (PR #2, commit c2e0fcc into main, client-confirmed; branch cleaned up). Root cause: `Log.carries()` had 0px landing-forgiveness while the death check had +12px (backwards for fairness) — guaranteed miss at certain tile boundaries, worse for 40px lily pads; secondary: lane positions read one frame stale at the landing check. New regression 4/4; existing BUG-1..5 suite 17/17. Not merged/pushed | 2026-07-24 | 2026-07-24 |
| SCOPE-03 | Client creative-direction change (verbatim, reacting to the M7.1 previews): "The image I think it's too hard pixel edges, please make it less pixelated. So the user can still see the object clearly." Contradicts the established "Arcade Pixel" hard-edge style (DESIGN §0/§1/§10, ASSET_MANIFEST Global Rule 5 "zero anti-aliasing", A3 check #1). Goal = READABILITY. Affects: per-asset native grid sizes (16x16 crush → larger, e.g. 32/48), the zero-AA rule + A3 check #1, the post-process params, and cross-consistency with the still-hard-edged programmatic chrome/HUD/particles + any programmatic-fallback sprites. INTENT FORK to resolve first: (A) still-retro-but-clearer (higher-res, light AA) vs. (B) fully smooth/illustrated (drop pixel-art). PM recommends (A). Action: HALT M7.2 full-gen; uiux-led A/B (regenerate 1-2 hero assets softer) → client confirms direction on sight → re-baseline manifest → resume. | Major (revises approved design system) | uiux (design decision) + user (taste confirm) | RESOLVED — client picked OPTION B (fully smooth/illustrated) 2026-07-24 after a 3-way A/B, having EXPLICITLY acknowledged the hard-pixel-chrome-over-soft-art clash before confirming. B ⇒ chrome/HUD/particle restyle is now REQUIRED (tracked as SCOPE-04 / milestone M7.7). uiux re-baselines DESIGN §0/§1/§10 + ASSET_MANIFEST (Global Rule 5, A3 #1, per-asset native sizes/palettes) per proposal §2; then M7.2 resumes | 2026-07-24 | — |
| SCOPE-04 | Consequence of SCOPE-03 (Option B): the programmatic chrome/HUD/score-panel/particles (DESIGN §1.3/1.4/1.6 + particle system) remain hard-edge pixel and will clash over smooth illustrated world art unless restyled to harmonize. Client accepted this tradeoff when picking B, so a coherent ship REQUIRES it — not optional, not a silent uiux side-effect. Scoped as explicit milestone M7.7 (uiux redesigns chrome/particle language for Option B; frontend reimplements in index.html). NOT a separate initiative — causally required by the M7 style change. | Major | uiux (design) + frontend (impl) | OPEN (scheduled M7.7; design can run parallel with M7.2 gen, impl lands with/after M7.4 integration) | 2026-07-24 | — |
| ISS-06 | DESIGN.md internal inconsistency on the canvas smoothing flag: §11.7 implies it's settled while §1.1 says it's explicitly M7.7's call. Frontend followed the technically-correct path at M7.4 (Option-B smooth WORLD art gets `imageSmoothingEnabled=true` scoped locally; HUD/particles stay `false`) — forcing false on smooth art would defeat the style change. Doc-only reconciliation, no code fix. | Minor | uiux | OPEN (reconcile at M7.7 chrome restyle; frontend's runtime behavior stands) | 2026-07-24 | — |
| ISS-07 | 3 automated test failures present after M7.4: TC-AC2-04, TC-AC2-07 (both AC-2 hop movement/feel) + TC-BREAK-01 (breakpoint). Frontend baseline-diffed them as pre-existing/unrelated to M7.4. BUT 2 are AC-2 and BUG-7 (just merged to main) altered hop/landing-forgiveness logic — so 'pre-existing relative to this branch' may actually mean 'introduced by the BUG-7 merge.' Must NOT ship a BUG-7 regression hidden behind 'pre-existing.' | Major (until root-caused) | tester | OPEN (M7.5: confirm whether these were GREEN before the BUG-7 merge; if BUG-7 caused them → regression to fix; if truly pre-existing → decide fix-now vs known-limitation) | 2026-07-24 | — |
| ISS-05 | M7.2 A5 curation findings (uiux formal pass, all 34 assets): (a) `tile-s2-road` NEW vertical-tiling fail (internal gradient bands at tile seam) beyond the 2 orchestrator pre-flagged; (b) `tile-s3-road`/`tile-s3-rail` orchestrator flags CONFIRMED; (c) `veh-s2-truck`/`veh-s3-truck`/`eagle-grabbed` baked-in ground/shadow patch under the subject (recurring Option-B failure mode, same as `chick-idle`'s earlier fix, Sections 2/6 negative prompts never got the strengthened terms); (d) `bg-s2-skyline` chroma-key never applied (0% transparent, raw source is clean — pipeline execution miss, reprocess-only fix, no new gen call needed). See DESIGN.md §10.6 for full per-asset reasoning. **UPDATE 2026-07-24 (uiux re-verification of the orchestrator's fix pass, DESIGN.md §10.6.10):** (d) `bg-s2-skyline` — FIXED, confirmed clean (border-pixel-MODE reprocess, 45.5% transparent, 0% key residue, registry-added). `eagle-grabbed` (part of c) — FIXED, confirmed clean on the 2nd regeneration attempt (pose now clearly swept-back/folded and distinct from `eagle-flying`, no ground patch, registry-added). `veh-s2-truck`/`veh-s3-truck` (remainder of c) — regenerated once with the strengthened negative prompt, but the ORIGINAL defect recurred in a NEW form on re-verification: both still carry a baked, opaque ground-contact-shadow ellipse under the wheels (recolored pale blue-grey on `veh-s2-truck`, tan/beige on `veh-s3-truck`, confirmed via pixel/connected-component scan and in-context composite over stage road colors) — same A3 check #7 failure, not eliminated, just changed appearance. These 2 remain OPEN/unfixed and still excluded from the asset registry. (a)/(b) tile items are unaffected by this update — still resolved via the existing permanent programmatic fallback per PLAN §7.3, no further action pending. **FINAL UPDATE 2026-07-24 (uiux final independent re-check of the orchestrator's mechanical fix round, DESIGN.md §10.6.3/§10.6.9/§10.6.11):** `veh-s3-truck` — CONFIRMED FIXED. The orchestrator's keep-largest-connected-component post-process is independently verified effective: pixel/connected-component scan now finds exactly 1 opaque component at every threshold tested (was 2: a 4111px body + a 375px disconnected shadow blob), alpha tapers cleanly to 0 by the last row, and an 8×-zoomed checkerboard + dark-asphalt composite show a clean underbody with no shadow. Moved from REJECT to ACCEPT and ADDED to the registry (30th entry). `veh-s2-truck` — STILL REJECT, independently re-confirmed. The orchestrator's characterization of "1 single component" (likely a false negative from using a near-zero/`>0` alpha threshold on a thin AA bridge) does not hold at meaningful thresholds (50/127/200 all show 2 separate opaque blobs: a 4489px body + a 506px pale grey-white shadow band spanning nearly the full 107px width, rows 57-63). Direct visual composite over both mid-grey and dark-asphalt confirms an unmistakable horizontal shadow band under the wheels, distinct in shape from the wheel-rim hub circles — this is a real defect, NOT the "wheel-rim highlight" false positive the automated heuristic suggested. Remains excluded from the registry; existing programmatic `Vehicle` draw stays wired. Registry now stands at 30/34 (28 ACCEPT + 2 ACCEPT-WITH-NOTE), with only `veh-s2-truck` (1 asset) still pending a real fix — the 3 tile fallbacks are zero-risk-by-design, not pending. | Major (b unshippable-as-tile, permanently on fallback / `veh-s2-truck` unshippable-as-generated, still pending) / Minor (a, permanently on fallback) / FIXED (`eagle-grabbed`, `bg-s2-skyline`, `veh-s3-truck`) | orchestrator (regen `veh-s2-truck` again, or post-process alpha-erase of the bottom shadow band with a lower connectivity threshold) | PARTIALLY RESOLVED — `bg-s2-skyline`, `eagle-grabbed`, `veh-s3-truck` all FIXED and in the DESIGN.md §10.6.11 registry (30/34); `veh-s2-truck` STILL OPEN (independently re-confirmed real defect, not a false positive); tile items (a)/(b) permanently on zero-risk programmatic fallback, not pending further work. Zero product risk throughout: `veh-s2-truck`'s existing programmatic draw stays wired (PLAN §7.2 Constraint 5) | 2026-07-24 | 2026-07-24 (partial: `eagle-grabbed`, `bg-s2-skyline`, `veh-s3-truck` fixed; `veh-s2-truck` still open) |

<!-- Stage 4 complete after 2 cycles: BUG-1..5 fixed & verified. BUG-6 cosmetic dev-tooling, won't-fix (not shipped). ISS-01/02/03 are human-playtest caveats, not ship blockers. No Critical/Major issue ships OPEN except ISS-01 which is inherently a human-judgement item, surfaced to the user. -->
<!-- 2026-07-24: SCOPE-01 logged — client-directed scope change authorizing Hugging Face MCP raster assets (inlined base64). Not a defect; decision recorded, PRD §5 amended. uiux owns DESIGN.md §10 follow-up. -->
<!-- 2026-07-24: SCOPE-02 logged — client-directed scope change authorizing HF MCP-generated audio SFX (inlined base64, decoded via Web Audio), parallel to SCOPE-01. PRD §5 + AC-11 amended (AMENDMENT #2). ENV-01 logged — subagent HF tool-access gap; M7 generation routes through the orchestrator. Both feed Milestone M7 (Asset Regeneration & Integration) in docs/PLAN.md. -->
<!-- 2026-07-24: ENV-02 logged — lm555 space unretrievable-output blocker on M7.1. PM decision: pivot image pipeline to FLUX.1-Schnell source + Global Rule 2 quantization post-process (DESIGN §10.2 path b, proven tools only). Validation-batch GO/NO-GO gate unchanged — sprite-fidelity risk is caught there before the full run. -->
<!-- 2026-07-24: M7.1 validation batch 3/4 PASS; bg-s1-hill-far fix = per-asset chroma-key color (ENV-02). PM recommends CONDITIONAL GO — proceed to M7.2 full generation once the hill re-run passes. -->
<!-- 2026-07-24: BUG-7 logged — client-reported river log/lily-pad landing too hard. Routed to frontend (sole owner of index.html; backend not used, PLAN §1) as an investigate-first task on its own branch fix/river-landing, sequenced to land BEFORE M7 integration (I1-I5) to avoid rebase friction. Independent of M7. -->
<!-- 2026-07-24: SCOPE-03 logged — client wants less-pixelated/clearer art, reversing the hard-edge design direction. M7.2 full generation HALTED; M7.1 conditional-GO PAUSED. uiux owns the design-system reconciliation (native size + AA rule + A3 + consistency); PM scopes; client confirms the (A)retro-clearer vs (B)smooth-illustrated fork on a visible A/B before any re-baseline. -->
<!-- 2026-07-24: SCOPE-03 RESOLVED — client locked Option B (smooth/illustrated). SCOPE-04 opened — required chrome/HUD/particle restyle (milestone M7.7). BUG-7 fixed on fix/river-landing, routed to tester; merges before M7 integration. -->
<!-- 2026-07-24: M7.2 images 34/34 done (uiux A3-curation pending; 2 tiles tile-s3-road/tile-s3-rail flagged likely-fail → programmatic fallback per §7.3). ENV-03 logged — MMAudio SFX blocked; retry via authenticated MCP, else defer with synth fallback. Raw-token handling DECLINED (Security). -->
<!-- 2026-07-24: ENV-03 DEFERRED — authenticated-MCP MMAudio structurally impossible (no mcp-server tag). Images ship first w/ synth-audio fallback; generated SFX tracked as fast-follow with 3 revisit paths. Client to be told. -->
<!-- 2026-07-24: ISS-05 logged — uiux M7.2 A5 formal curation pass (DESIGN.md §10.6): 25 ACCEPT / 2 ACCEPT-WITH-NOTE / 3 REJECT-FALLBACK-TO-PROGRAMMATIC (tiles) / 4 REJECT-REGENERATE-NEEDED. Confirmed both orchestrator tile flags + found 1 more (tile-s2-road); found 3 baked-ground-shadow defects (veh-s2-truck, veh-s3-truck, eagle-grabbed) and 1 unkeyed-transparency defect (bg-s2-skyline). All 7 non-accepted assets are fallback-safe — zero product risk, existing programmatic draws stay wired. Asset registry for the 27 accepted assets is in DESIGN.md §10.6.10, ready for frontend M7.4 integration. -->
<!-- 2026-07-24: ISS-05 re-verified by uiux after the orchestrator's fix pass (DESIGN.md §10.6.10): 2 of 4 regenerated/reprocessed assets confirmed FIXED (`bg-s2-skyline` reprocess, `eagle-grabbed` 2nd regen) and added to the §10.6.11 asset registry (27 -> 29 accepted). The other 2 (`veh-s2-truck`, `veh-s3-truck`) still fail A3 check #7 on re-verification — the original green/tan ground patch is gone but a new baked contact-shadow ellipse (pale blue-grey / tan respectively) remains, confirmed via pixel scan + in-context composite. Both remain excluded from the registry, existing programmatic Vehicle draws stay wired; zero product risk. ISS-05 marked PARTIALLY RESOLVED. -->
<!-- 2026-07-24: BUG-7 MERGED (PR #2 c2e0fcc). M7.2 curation complete — 33/34 images are integration candidates (4 rejects fixed by orchestrator: bg-s2-skyline script-fix, veh-s2/s3-truck regen+component-isolation, eagle-grabbed regen); 3 tiles (tile-s2-road, tile-s3-road, tile-s3-rail) intentionally programmatic-fallback. uiux to do a final registry sync (DESIGN §10.6.11) so the 2 trucks read ACCEPT; frontend scaffold can build in parallel. -->
<!-- 2026-07-24: uiux FINAL registry sync complete (DESIGN.md §10.6.11): independently re-verified veh-s2-truck/veh-s3-truck one more time (pixel/connected-component scan + visual composite over mid-grey/dark-asphalt, not a rubber-stamp of the orchestrator's characterization). RESULT: veh-s3-truck CONFIRMED FIXED (keep-largest-component post-process worked, moved to ACCEPT, added to registry as 30th entry). veh-s2-truck STILL FAILS (real shadow-band defect independently confirmed, NOT the wheel-rim-highlight false positive it was framed as; the orchestrator's "1 component" reading was an artifact of a near-zero alpha threshold — at threshold >=50 the shadow blob separates cleanly from the body every time). Final registry: 30/34 assets (28 ACCEPT + 2 ACCEPT-WITH-NOTE), all file paths verified to exist on disk; 3 tiles + veh-s2-truck stay on programmatic fallback. ISS-05 updated to PARTIALLY RESOLVED with this final state. -->
<!-- 2026-07-24: FINAL registry (DESIGN §10.6.11) authoritative — 30/34 generated assets ACCEPTED (28 ACCEPT + 2 ACCEPT-WITH-NOTE), all 30 paths verified on disk. 4 PROGRAMMATIC FALLBACKS (zero risk): veh-s2-truck (real shadow-band defect, caught by uiux's multi-threshold re-verify — orchestrator's single-threshold check had it as false-accept) + 3 tiles (tile-s2-road, tile-s3-road, tile-s3-rail). This 30-asset set is what frontend M7.4 wires. -->
<!-- 2026-07-24: M7.4 DONE on feature/asset-regen (3 commits, unmerged): 30/34 wired fallback-first, 4 programmatic fallbacks, zero-network confirmed, 68KB->593KB (~509KB base64, R11 accepted). ISS-06 (smoothing-flag doc inconsistency, uiux/M7.7) + ISS-07 (3 test fails, tester must confirm not a BUG-7 regression) logged. Doc-commit hygiene: PRD/PLAN/ISSUES + assets uncommitted — commit on branch before PR. -->
