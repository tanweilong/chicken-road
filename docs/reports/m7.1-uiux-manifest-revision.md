# uiux — M7.1 revision: ASSET_MANIFEST.md pivot for ENV-02

Date: 2026-07-24
Agent: uiux (Sonnet 5)
Scope: SURGICAL revision to `docs/ASSET_MANIFEST.md` Global Rule 1 + per-asset
prompt backdrop instructions only (per PM decision, ISSUES.md ENV-02, PLAN.md
§7.9). NOT a re-spec: asset list (A1), validation criteria (A3), validation
batch (A4), and the SFX section are untouched.

## 🟢 START

Inputs read in full before starting: `docs/reports/ISSUES.md` ENV-02 entry;
`docs/PLAN.md` §7.9 (pipeline decision, R15); the existing
`docs/ASSET_MANIFEST.md` (all 618 lines, written by uiux in M7.0);
`docs/DESIGN.md` §3 headings (confirming §3 Stage Palettes is the hex
source referenced throughout the manifest).

TODO checklist:
- [x] Rewrite Global Rule 1: mandate `evalstate/flux1_schnell` + mandatory
      Global Rule 2 quantization pass instead of `lm555/game-asset-generator`;
      keep `lm555` referenced as "revisit if MCP access is fixed."
- [x] Add the R15 risk note (tiny sprites may read muddier than
      hand-authored `PIXEL_MAPS`; validation batch tests this; per-category
      GO/NO-GO escalation to the user if so) under Global Rule 1.
- [x] Walk every per-asset prompt in the manifest and ensure each requests a
      solid, uniform, keyable chroma-key backdrop.
- [x] Spot-check that every asset has an explicit target-palette hex list
      for Rule 2c.
- [x] Leave A1 (asset list), A3 (validation criteria), A4 (validation batch),
      and the SFX section unchanged.

## 🔵 PROGRESS

1. **Global Rule 1 rewritten.** New text: mandates `evalstate/flux1_schnell`
   as the source generator, used together with — never without — the
   existing mandatory Global Rule 2 post-process (chroma-key → nearest-
   neighbor downscale to native grid → palette-quantize to the exact
   per-asset hex list → native-grid transparent PNG). Explicitly states this
   is not a reversal of the DESIGN §10.2 finding that raw FLUX output alone
   is a style-fit failure — the Rule 2 pass is what imposes the pixel grid
   deterministically in our own code, independent of the model.
   `lm555/game-asset-generator` is kept in the rule text, labeled
   "DEPRECATED FOR NOW, not deleted," with the exact ENV-02 failure mode
   (unretrievable `PIL.Image` via authenticated MCP; quota failure via
   anonymous `gradio_client`; token-attachment correctly refused per
   Security Rules) and an explicit "revisit only if/when its MCP access is
   fixed" instruction.
2. **R15 note added** under Global Rule 1, verbatim in spirit with PLAN
   §7.9's risk register: tiny generated sprites (chicken/vehicles/corn/
   eagle) downscaled to ~16×16 may read muddier than hand-authored
   `PIXEL_MAPS`; this is exactly what the A4 validation batch is designed to
   test; a sprite-specific shortfall should be surfaced to the user as a
   per-category decision (hand-authored sprites + generated backgrounds/
   props is a fully supported outcome under fallback-first rendering), not
   an automatic whole-pipeline NO-GO. Cross-referenced this same nuance in
   the A4 GO/NO-GO paragraph (added one clarifying sentence there — did not
   touch the batch selection or pass/fail mechanics themselves).
3. **Per-asset prompt backdrop pass** — went through every section:
   - Section 1 (chicken): `chick-idle`, `chick-hop` — replaced "transparent
     background" with "pure solid #00FF00 green-screen backdrop, flat and
     uniform, no gradient, no scenery, no shadow."
   - Section 2 (vehicles): shared prompt template — same replacement,
     applies to all 7 vehicle rows via the template.
   - Section 3 (river objects): `log-s1`/`log-s2` template and `lily-pad` —
     same replacement; added a one-line note on `lily-pad` and `corn-pickup`
     (Section 5) flagging that the subject's own base green (#3C9D45 /
     #7ABF4C) is distinct enough from pure #00FF00 that the model shouldn't
     be allowed to drift the backdrop or subject hue toward each other
     (protects the Rule 2a chroma-key from a bad key).
   - Section 4 (train): `train-car`, `signal-housing` — same replacement.
   - Section 5 (corn): `corn-pickup` — same replacement + the green-
     collision note above.
   - Section 6 (eagle): `eagle-flying` — same replacement; `eagle-grabbed`
     inherits it automatically ("same prompt, swap...") plus an explicit
     "backdrop unchanged" line for clarity.
   - Section 7 (ground tiles): **left untouched by design** — these are
     full-fill opaque textures explicitly excluded from the A3 #3
     transparency requirement (not chroma-keyed), so no backdrop instruction
     applies; added one clarifying sentence to the section intro stating
     this explicitly so a future reader doesn't wonder why tiles are
     inconsistent with the other sections.
   - Section 8 (parallax): all 6 rows (`bg-s1-hill-far/near/sun`,
     `bg-s2-skyline/building`, `bg-s3-smokestack`) — same replacement, using
     "backdrop" (not "background") in the added phrase specifically to avoid
     confusion with these assets themselves being background/parallax
     elements.
   - A4 validation batch write-up — updated the "Space:" lines for the 4
     ready-to-execute entries to name `evalstate/flux1_schnell` + Rule 2
     (was `lm555/game-asset-generator`), and noted `tile-s1-grass` has no
     backdrop instruction (consistent with Section 7).
4. **Hex-list spot-check (item 3 of the task)** — confirmed every one of the
   34 image-asset rows already carries an explicit target-palette hex list
   for Rule 2c, either as a dedicated "Palette lock:" line (chicken,
   vehicles) or embedded directly in the prompt/table (river objects, train,
   corn, eagle, tiles' "Per-row fill-ins," parallax). No gaps found — see
   🟠 ISSUES below for the one non-blocking observation.

## 🟠 ISSUES

- No asset is missing its Rule 2c target-palette hex list (item 3 of the
  task) — nothing to flag as a gap there.
- **Non-blocking observation (not fixed, out of this task's authorized
  scope):** Global Rule 2a's parenthetical `remove_green_background` names
  the specific built-in operation from the now-deprecated
  `lm555/game-asset-generator` space. With the pivot to
  `evalstate/flux1_schnell`, chroma-keying is now performed by our own
  post-process code, not a space-provided op — the parenthetical is now
  slightly stale/misleading (reads as if a tool-provided function is still
  being invoked). The task explicitly scoped this edit to "Global Rule 1 +
  prompt backdrop instructions only," so I did not touch Rule 2's text.
  Flagging for the orchestrator/PM: a one-word tweak (e.g. drop the
  parenthetical or generalize it to "our chroma-key post-process") would be
  a trivial, purely-cosmetic follow-up whenever Rule 2 is next touched — not
  worth its own revision cycle on its own.

## ✅ DONE

- [x] Global Rule 1 rewritten: image path → `evalstate/flux1_schnell` +
      mandatory Rule 2 quantization; `lm555` kept as "revisit if MCP access
      is fixed," not deleted.
- [x] R15 note added under Global Rule 1 (and cross-referenced once more in
      the A4 GO/NO-GO paragraph).
- [x] Every per-asset prompt requiring chroma-key (all sprites, vehicles,
      river objects, train/signal, corn, eagle, parallax units — 28 of the
      34 image rows) now explicitly requests a "pure solid #00FF00
      green-screen backdrop, flat and uniform, no gradient" (or the
      inherited equivalent). Ground tiles (11 rows, but overlapping with
      the 34 count differently — see manifest for exact row accounting)
      correctly excluded per A3's own transparency-exemption for opaque
      tiles.
- [x] Hex-list spot-check complete — no gaps found (see 🟠 ISSUES for one
      non-blocking cosmetic note, not a gap).
- [x] A1, A3, A4 (mechanics), SFX section confirmed unchanged except the
      4 "Space:" lines in A4 (mechanical rename only, no criteria changed).

**Deliverable:** `docs/ASSET_MANIFEST.md` (revised in place, full rewrite via
Write tool since no diff-only edit tool was available in this run — content
verified against the original read before writing, only the scoped sections
changed).

**Handoff to orchestrator:** ready to re-run the M7.1 validation batch
(`chick-idle`, `veh-s1-car`, `bg-s1-hill-far`, `tile-s1-grass`) against
`evalstate/flux1_schnell` + the Rule 2 post-process pipeline, using the
updated prompts (now requesting the #00FF00 backdrop) from the manifest.
GO/NO-GO gate and pass/fail mechanics are unchanged from M7.0.
